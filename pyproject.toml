[project]
name = "smartbin-deeplearning"
version = "0.1.0"
description = "Smart bin deep learning project"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.36",
    "tensorflow>=2.15",
    "pillow>=10.0",
    "numpy>=1.24",
]

[tool.uv]
package = false
